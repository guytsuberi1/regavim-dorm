/* seed-data.js — נתוני פתיחה מוטמעים (אופציונלי).
   אם הענן והמאגר המקומי ריקים — האפליקציה נטענת עם הנתונים שכאן.
   כרגע ריק: הכיתות ושאלון ברירת המחדל מוגדרים ב-store.js (defaultData). */
window.__SEED_DATA = null;
