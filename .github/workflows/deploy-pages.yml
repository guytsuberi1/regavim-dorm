name: Deploy dorm site to GitHub Pages

# מפרסם את dorm-site/ לענף gh-pages — GitHub Pages מגיש ממנו את האתר.
# (הפרסום דרך ענף ולא דרך Pages Actions כדי שלא תידרש הפעלה ידנית בהגדרות הריפו.)
on:
  push:
    branches: [main]
    paths:
      - 'dorm-site/**'
      - '.github/workflows/deploy-pages.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: dorm-site
          publish_branch: gh-pages
